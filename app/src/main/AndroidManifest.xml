<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login/Registration Activities -->
        <activity android:name=".LoginActivity" android:screenOrientation="portrait" />
        <activity android:name=".RegisterActivity" android:screenOrientation="portrait" />
        <activity android:name=".PasswordResetActivity" android:screenOrientation="portrait" />
        <activity android:name=".EmailVerificationActivity" android:screenOrientation="portrait" />

        <!-- Game Activities -->
        <activity android:name=".KorakPoKorakActivity" android:screenOrientation="portrait" />
        <activity android:name=".MojBrojActivity" android:screenOrientation="portrait" />
        <activity android:name=".SkockoActivity" android:screenOrientation="portrait" />
        <activity android:name=".KoZnaZnaActivity" android:screenOrientation="portrait" />
        <activity android:name=".SpojniceActivity" android:screenOrientation="portrait" />
        <activity android:name=".AsocijacijeActivity" android:screenOrientation="portrait" />

        <!-- Profile Activity -->
        <activity android:name=".ProfileActivity" android:screenOrientation="portrait" />

        <!-- Notifications Activity -->
        <activity android:name=".NotificationsActivity" android:screenOrientation="portrait" />

    </application>

</manifest>